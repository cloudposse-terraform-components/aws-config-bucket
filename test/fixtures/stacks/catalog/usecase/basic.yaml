components:
  terraform:
    aws-config-bucket/basic:
      metadata:
        component: target
      vars:
        enabled: true
        name: "test"
        noncurrent_version_expiration_days: 180
        noncurrent_version_transition_days: 30
        standard_transition_days: 60
        glacier_transition_days: 180
        expiration_days: 365
